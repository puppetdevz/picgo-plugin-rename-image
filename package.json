{
  "name": "picgo-plugin-rename-image",
  "version": "2.0.6",
  "description": "对上传的图片进行重命名",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/puppetdevz/picgo-plugin-rename-image#readme",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "dev": "tsc -w -p .",
    "patch": "npm version patch && git push origin main && git push origin --tags",
    "minor": "npm version minor && git push origin main && git push origin --tags",
    "major": "npm version major && git push origin main && git push origin --tags"
  },
  "keywords": [
    "picgo",
    "picgo-gui-plugin",
    "picgo-plugin"
  ],
  "author": "puppet",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.59.11",
    "@typescript-eslint/parser": "^5.59.11",
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "picgo": "^1.5.3",
    "prettier": "^2.8.8",
    "typescript": "^5.1.3"
  }
}
